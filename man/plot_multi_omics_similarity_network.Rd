% Generated by roxygen2: do not edit by hand
% Please edit documentation in
%   R/14_data_visualization_plot_multi_omics_similarity_network.R
\name{plot_multi_omics_similarity_network}
\alias{plot_multi_omics_similarity_network}
\title{Plot Multi-Omics Functional Module Similarity Network}
\usage{
plot_multi_omics_similarity_network(
  object,
  module_size_cutoff = NULL,
  module_id = NULL,
  node_shapes = c(gene = 21, metabolite = 24, pathway = 22),
  edge_colors = c(`TF-target` = "#E15759", PPI = "#76B7B2", Reaction = "#B07AA1",
    molecule_pathway = "#F1CE63", pathway_similarity = "#439222", diffusion_similarity =
    "grey80"),
  node_size = 4,
  label_size = 3,
  show_rwr_edge = TRUE,
  show_labels = TRUE,
  use_llm_label = TRUE,
  show_node_labels = FALSE,
  node_label_size = 2.5,
  module_circle_radius = 10,
  module_scale_factor = 1.2,
  outline_expand = 5,
  seed = 42
)
}
\arguments{
\item{object}{\code{list}. Direct output of \code{\link[=merge_multi_omics_nodes]{merge_multi_omics_nodes()}} or
\code{\link[=llm_interpret_module]{llm_interpret_module()}}, containing \code{graph_data}, \code{functional_module_result}, and \code{result_with_module}.}

\item{module_size_cutoff}{\code{numeric(1)} or \code{NULL}. Retain only modules whose
\code{module_content_number} is \strong{>=} this value. Mutually exclusive with
\code{module_id}. When both are \code{NULL} all modules are shown. Default \code{NULL}.}

\item{module_id}{\code{character} vector or \code{NULL}. Retain only the listed module
IDs (e.g. \code{c("Functional_module_1", "Functional_module_3")}). Mutually
exclusive with \code{module_size_cutoff}. Default \code{NULL}.}

\item{node_shapes}{Named integer vector of point shapes for each node type.
Use fill-able shapes (21 = circle, 22 = square, 24 = triangle up).}

\item{edge_colors}{Named character vector of colours for each edge type.}

\item{node_size}{\code{numeric(1)}. Base point size. Default \code{4}.}

\item{label_size}{\code{numeric(1)}. Module label text size (pt). Default \code{3}.}

\item{show_rwr_edge}{\code{logical(1)}. Whether to display diffusion-similarity
edges in addition to knowledge edges. Default \code{TRUE}.}

\item{show_labels}{\code{logical(1)}. Whether to display module-level labels
at the centroid of each module. Default \code{TRUE}.}

\item{use_llm_label}{\code{logical(1)}. When \code{TRUE} (default), module labels use
the LLM-generated name (\code{llm_module_name}) when available, falling back to
the module ID. When \code{FALSE}, always use the module ID as the label.}

\item{show_node_labels}{\code{logical(1)}. Whether to display individual node ID
labels on each node. Default \code{FALSE}.}

\item{node_label_size}{\code{numeric(1)}. Text size for node labels (pt).
Default \code{2.5}.}

\item{module_circle_radius}{\code{numeric(1)}. Radius of the large circle on which
module centroids are placed. Increase to spread modules further apart;
ignored when only one module is shown. Default \code{10}.}

\item{module_scale_factor}{\code{numeric(1)}. Controls how spread-out nodes are
within each module. Larger values expand each module's internal layout;
the spread also scales with \verb{log2(node count)}. Default \code{1.2}.}

\item{outline_expand}{\code{numeric(1)}. Extra space (mm) added around each
convex-hull outline. Default \code{5}.}

\item{seed}{\code{integer(1)}. Random seed for the Fruchterman-Reingold layout.
Default \code{42}.}
}
\value{
A \code{ggplot} / \code{ggraph} object.
}
\description{
Creates a similarity network visualisation of multi-omics functional modules.
}
